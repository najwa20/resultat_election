<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aziz_najwa_dsi32_g1.resultat_election">

    <application
        android:icon="@mipmap/election"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/election"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".cree_compte" />
        <activity android:name=".ajouter_election"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".ajouter_choix"/>
        <activity android:name=".list_choix_admin"/>
        <activity android:name=".liste_election_admin"/>
        <activity android:name=".fin_elec"/>
        <activity android:name=".res_elec"/>
        <activity android:name=".list_choix"/>
        <activity android:name=".liste_election"/>
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>